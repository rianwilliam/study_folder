// Importar bibliotecas
#include <iostream>

// Usado para não ser necessário colocar "str::" em cada operação.
using namespace std;

int main(int argc, char** argv) {
    // Cout: Console output
    cout << "Olá, mundo" << "\n";

    // Definindo tipo da variável
    int n = 10;
    double n3 = 10.50;
    bool myBool = true;
    char myChar = 't';

    // Coloca-se F no final para dizer que é flutuante
    float n2 = 10.0f;

    // Define tamanho de coisas (não pode ser negativo)
    size_t mySizeT = 0;

    // Inteiro que não pode ser negativo
    unsigned int myUnit = 10;

    // Usar no windows
        // system("pause");
    return 0;
}