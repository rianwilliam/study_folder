
#include <stdio.h>

int main(void) {

    int num, fatorial;
    printf("Insira um numero para fatorar");
    scanf("%d", &num);

    return 0;
}

int fatorial( int num ) {
    if ( num == 0 ) {
        return 1;
    }
}
